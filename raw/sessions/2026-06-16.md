# Session — 2026-06-16

## Tool-discoverability / normalization pass (continued an interrupted plan)

**Origin:** a user gave a task list to populate a blank project; the model looped
single-item `add_task` instead of `bulk_add_tasks`. Root cause = discoverability, not a
missing capability. Plan = full normalization of the tool surface.

**State found on resume:** Phase 8 (commit `5f3a6fc`) had already added the completeness
bulk tools + a `get_tool_guide()` meta-tool (the plan's `list_capabilities`, renamed) →
104 tools. Working tree already had P0 `SERVER_INSTRUCTIONS`, the `assign_resource`
cross-ref, and the 2 EXPO-2030 fixes. So P0/P2 were effectively done.

**Completed this session (server.py + README.md, uncommitted):**
- **P1** — confirmed all 10 single tools redirect to their `bulk_*` (Phase 8 did the
  single side); added a reverse "prefer this over looping X — whenever you have 2+ …"
  line to all 11 bulk tools.
- **P3** — annotations on **all 101 tools** via a one-off generator script
  (`_annotate.py`, deleted): `from mcp.types import ToolAnnotations`, `title` Title-Cased
  from the function name (acronym map RAG/WBS/CSV/XML/JSON/ID/EV), and hints by
  classification. Tally: 40 readOnly / 13 destructive / 26 idempotent / 22 plain = 101.
  Script asserted full coverage (no tool unclassified).
- **P4** — added `Args:`/`Returns:` to the 8 param-taking tools that lacked an Args block
  (`get_task`, `get_tasks_by_resource`, `get_tasks_by_rag`, `close_project`, `delete_task`,
  `remove_predecessor`, `get_task_dependencies`, `delete_resource`). Clean one-liner read
  tools left as-is (already valid). EXPO refs already gone.
- **P5 (breaking)** — removed `import_xml`, `export_xml`, `search_tasks`. No internal or
  test callers. Folded behaviour into parent docstrings: `open_project` opens `.xml`,
  `save_project_as(format="xml")`, `get_tasks(keyword=…)`. Updated `get_tool_guide`
  category map, README inventory/counts (104→101), and added a "Usage & discoverability"
  section + a Phase 9 row + breaking-change note.

**Verification:** `python -m py_compile server.py` passes; a throwaway introspection test
(`_introspect_test.py`, deleted) confirmed: count 101; wrappers absent; every tool has a
non-empty description + title; no EXPO 2030; each single names its bulk; read tools
`readOnlyHint=True`; destructive list `destructiveHint=True`; file-writers not read-only;
server `instructions` non-empty with the batching rule. All passed.

**Note:** `ToolAnnotations.title` stays None — the FastMCP `@mcp.tool(title=...)` arg sets
the top-level `Tool.title`, not the annotation's title. Check `Tool.title` when introspecting.

**Not done:** commit + push (awaiting go-ahead); real-world acceptance test in Claude
Desktop (the actual success criterion); test suites need a live MS Project on Windows.
